REPLY_FILE: /Users/yasyf/Code/.lanes/cc-patch-adopt/.codex-reply.md

Rebase result:

- `git fetch origin` succeeded.
- `git rebase origin/main` reported `Current branch lane/daemonkit-v020-adopt is up to date.`
- No conflicts occurred. The StableProgram adoption commit and legacy-state error commit both remain in the lane.

Dependency resolution result:

- With `GOPRIVATE=github.com/yasyf/daemonkit`, `go get github.com/yasyf/daemonkit@v0.20.0` and `go mod tidy` both succeeded.
- `go list -m github.com/yasyf/daemonkit` reports `github.com/yasyf/daemonkit v0.20.0`.
- `go mod verify` reports `all modules verified`.
- The only tidy delta was replacing daemonkit v0.19.1 hashes with the released v0.20.0 hashes in `go.sum`.

Full test summary:

- PASS: `go build ./...`
- PASS: repo-mandated `task test` (`go test -race -count=1 ./...`): 10 test-bearing packages passed, 0 failed; 6 packages had no test files.
- Runner tail:

  ```
  ok  	github.com/yasyf/cc-patch/internal/registry	99.060s
  ok  	github.com/yasyf/cc-patch/internal/store	1.946s
  ?   	github.com/yasyf/cc-patch/internal/version	[no test files]
  ```

- PASS: `golangci-lint run ./...` and repo wrapper `task lint`, both `0 issues.`
- PASS: repo wrapper `task build`.
- No adoption call-site or test-expectation fix was needed.

Commits:

- New: `eef6355 build: resolve daemonkit v0.20.0 checksums`
- Preserved: `d6302d7 fix(store): name the remedy for legacy pre-v0.5.0 state`
- Preserved: `7c5fca7 daemon: register launchd agent at daemonkit stable program path`
- No push was performed.

Release convention:

- changelog: `CHANGELOG.md` follows Keep a Changelog and SemVer, keeps an empty `[Unreleased]` section, uses dated headings for current releases, and maintains compare/tag link lines at the bottom. Before release, add the feature/fix under Unreleased and advance the comparison links.
- version-source: Git tags are the release source of truth; there is no version file. `internal/version.Version` defaults to `dev`; Task builds stamp `git describe`, while GoReleaser stamps `.Version` from the tag through ldflags.
- latest-tag: `v0.11.1`, `v0.11.0`, `v0.10.0`
- tag-format: `v`-prefixed SemVer (`vX.Y.Z`, with valid SemVer prerelease/build suffixes accepted). The current trust gate requires one annotated tag object signed by the fleet release key.
- release-gates: A `v*` push invokes the exact-pinned reusable `release-go.yml`. It requires the signed annotated tag to directly name `github.sha` on `main`; GitHub API and local GPG verification must agree, and remote tag movement is rejected. There is no explicit CI-green/status-check gate in the release workflow. It requires the signing/notarization secrets, runs GoReleaser v2 to build without publishing, verifies/stages an exact draft, publishes the release, then publishes the immutable native Homebrew cask from `.goreleaser.yaml` for non-prereleases. This repo does not enable the reusable workflow's `render-formula` path.
- graphite: no — `/Users/yasyf/Code/cc-patch/.git/.graphite_repo_config` does not exist.
- jj: yes — `/Users/yasyf/Code/cc-patch/.jj` exists, so the main checkout is colocated jj/git.

Suggested next tag: `v0.12.0` — the stable daemon program path is a feature-level behavior change, while the store diagnostic and dependency resolution fit into that minor release.
